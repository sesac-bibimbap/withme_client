const Main = () => {
  // const { data, isLoading } = useProfileQuery();

  return (
    <>
      메인페이지얌!~
      {/* {!isLoading ? (
        <>
          <div>Main</div>
          <div>{data.email}</div>
        </>
      ) : (
        <p>Loading...</p>
      )} */}
    </>
  );
};

export default Main;
